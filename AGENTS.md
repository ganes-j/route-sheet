# route-sheet — agent guide

> **Status: skeleton.** Filled in by the setup unit.

This repo documents and ships a plan-time executor-routing system for agentic coding. If you are an agent reading this on behalf of a user:

- To **explain** the system: read `docs/` (start with `docs/architecture.md`).
- To **install** it: follow `SETUP.md` — it is written for you, with preflight probes, consent gates, and per-step verification.
- To **adapt** it: the runnable artifacts live in `templates/`, `skills/`, `bin/`, and `hooks/`.
